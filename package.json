{
  "name": "authentication-server",
  "version": "0.0.1",
  "description": "An authentication server for our web services.",
  "main": "index.js",
  "scripts": {
    "start": "cd dist && node index.js",
    "dev": "cd src && nodemon index.ts",
    "build": "tsc --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.43",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.16",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3"
  }
}
